Memory.hdl
HDL code to simulate the working of the memory segment of the computer. This includes the RAM, Screen and Keyboard. 

CPU.hdl
Simulates the working of the CPU which includes the Address and Data registers, ALU and the program counter.

Computer.hdl
Provides the connections between the major parts of the computer like the CPU, Memory and Instruction memory (ROM). Has only one input, rest
